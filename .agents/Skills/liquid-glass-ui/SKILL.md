---
name: liquid-glass-ui
description: Liquid Glass 4.0 Pro design system rules and CSS parameters
---

# Liquid Glass 4.0 Pro UI Skill

## Design System Parameters
1. **Glassmorphic Containers (`.liquid-glass`):**
   - Background: `linear-gradient(135deg, rgba(28, 25, 23, 0.65) 0%, rgba(15, 23, 42, 0.45) 100%)`
   - Backdrop Filter: `blur(24px) saturate(180%)`
   - Border: `1px solid rgba(245, 158, 11, 0.22)`
2. **Ambient Lighting:**
   - Glowing background orbs (`filter: blur(100px)`) with smooth CSS floating animations.
3. **Typography Gradients:**
   - `from-yellow-200 via-amber-400 to-amber-500`
